# -*- mode: python -*-
a = Analysis(['TimeMachine.py'],
             pathex=['E:\\workroom\\TimeMachine'],
             hiddenimports=[],
             hookspath=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          a.binaries,
          a.zipfiles,
          a.datas,
          name=os.path.join('dist', 'TimeMachine.exe'),
          debug=False,
          strip=None,
          upx=True,
          console=False )
app = BUNDLE(exe,
             name=os.path.join('dist', 'TimeMachine.exe.app'))
